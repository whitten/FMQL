class FMSDescribeReply(object):

    """
    A peer of (FMQL)DescribeReply, this allows you to easily process equivalent data stored as RDF in a triple store
    
    Note: big differences - natively SPARQL doesn't have the notion of "context" ie/ qualifying (often provenance) data or that all URIs have rdfs:labels.  
    """
    pass